node_modules/
dist/
build/
*.log
.DS_Store

# Per-job runtime artifacts (the example fixture is force-added in a later phase)
workspace/*
!workspace/.gitkeep

# Local env
.env
.env.local

# Runtime-verification transient data (keep probe.mjs + results.json as evidence)
.verify/workspace/
.verify/*.log
